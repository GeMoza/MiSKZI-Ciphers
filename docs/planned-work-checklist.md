# Планируемые работы

Этот файл фиксирует рабочий чеклист по проекту в рекомендуемом порядке закрытия задач.
Логика порядка такая:

1. Сначала выровнять документацию и фактическое состояние проекта.
2. Затем стабилизировать проверки и интерфейс.
3. После этого закрыть пробелы в уже реализованных шифрах.
4. И только потом переходить к добавлению новых шифров и алгоритмов из следующих ПЗ.

## 1. Аудит и выравнивание документации

- [x] Обновить корневой `README.md` по фактическому списку реализованных шифров.
- [x] Отразить в `README.md`, что текущий быстрый запуск и запуск тестов требуют уточнения по среде.
- [x] Проверить согласованность `README.md` с `src/`, `data/`, `tests/` и `docs/`.
- [x] Проверить согласованность `docs/README.md` с текущим набором файлов в `docs/`.
- [x] Исправить `docs/methodics-mapping.md` по фактическому состоянию `src/`, `data/` и `tests/`.
- [x] Убрать из `docs/methodics-mapping.md` ссылки на отсутствующие данные, если они не соответствуют репозиторию.
- [x] Пересобрать сопоставление ПЗ-1..ПЗ-5 по локальным PDF-методичкам.
- [x] Добавить в `docs/methodics-mapping.md` отдельные разделы или примечания для ПЗ-6 и ПЗ-7.
- [x] Проверить, не устарели ли формулировки в `docs/adding-a-new-cipher.md` относительно реального рабочего процесса.
- [x] Согласовать `docs/changelog.md` с фактической версией проекта из `pyproject.toml`.

## 2. Матрица покрытия и инвентаризация шифров

- [ ] Зафиксировать единый реестр всех текущих `cipher_id` из `src/miskzi_ciphers/ciphers/`.
- [ ] Для каждого реализованного шифра отметить наличие `data/<cipher>/variants.json`.
- [ ] Для каждого реализованного шифра отметить наличие предметных векторных тестов.
- [ ] Для каждого реализованного шифра отметить наличие UI-локализации в `src/miskzi_ciphers/ui/i18n.py`.
- [ ] Для каждого реализованного шифра отметить наличие упоминания в методичках.
- [ ] Отдельно отметить шифры, которые реализованы, но не имеют `data/`.
- [ ] Отдельно отметить шифры, которые реализованы, но не имеют предметных векторных тестов.
- [ ] Отдельно отметить шифры, которые есть в методичках, но пока не реализованы.

## 3. Стабилизация автопроверок и среды

- [ ] Разобрать реальные падения текущих тестов.
- [ ] Разобрать ошибки среды выполнения тестов на Windows и отделить их от ошибок кода.
- [ ] Добиться воспроизводимого запуска `pytest` в рабочей среде проекта.
- [ ] Уточнить в документации, как правильно запускать тесты в используемом окружении.
- [ ] Проверить, что общие контрактные тесты действительно покрывают все текущие шифры.
- [ ] Проверить, что тесты схемы данных и метаданных покрывают все актуальные `variants.json`.

## 4. Проверка и стабилизация интерфейса

- [ ] Проверить Playground для всех шифров с параметрами.
- [ ] Проверить Playground для шифров без параметров.
- [ ] Проверить загрузку `meta.free_text` в интерфейсе.
- [ ] Проверить загрузку `meta.raw_key_example` в интерфейсе.
- [ ] Проверить синхронизацию Form и Raw JSON для ключей.
- [ ] Проверить отображение таблицы параметров из `describe().params`.
- [ ] Проверить Data Manager на чтение, редактирование и сохранение `variants.json`.
- [ ] Проверить Data Manager на чтение, редактирование и сохранение `meta`.
- [ ] Зафиксировать найденные UI-дефекты в отдельном списке задач.

## 5. Закрытие пробелов в уже реализованных шифрах

- [ ] Проверить, для каких реализованных шифров отсутствуют `data/<cipher>/variants.json`.
- [ ] Проверить, для каких реализованных шифров отсутствуют контрольные примеры из методичек.
- [ ] Проверить, для каких реализованных шифров отсутствуют отдельные векторные тесты.
- [ ] Принять решение по `bacon`: добавить данные или зафиксировать его особый статус в документации.
- [ ] Проверить, что все текущие `variants.json` реально соответствуют контракту своих шифров.
- [ ] Проверить, что `meta.raw_key_example` у существующих шифров можно использовать в UI без ручных правок.

## 6. Подготовка к добавлению новых тем из ПЗ-06

- [ ] Подготовить карточку требований для `rsa`.
- [ ] Подготовить карточку требований для `elgamal`.
- [ ] Решить, укладываются ли `rsa` и `elgamal` в текущий контракт `describe()/parse_key()/encrypt()/decrypt()`.
- [ ] Решить, нужен ли для `rsa` отдельный формат учебных данных и ключей.
- [ ] Решить, нужен ли для `elgamal` отдельный формат учебных данных и ключей.
- [ ] Подготовить список методических контрольных примеров для ПЗ-06.

## 7. Подготовка к добавлению новых тем из ПЗ-07

- [ ] Подготовить карточку требований для `magma`.
- [ ] Подготовить карточку требований для `rc5`.
- [ ] Подготовить карточку требований для `rc6`.
- [ ] Подготовить карточку требований для `kuznechik`.
- [ ] Отдельно определить статус темы `feistel_network`: демонстрационный модуль, отдельный алгоритм или базовая схема для других реализаций.
- [ ] Решить, укладываются ли шифры ПЗ-07 в текущий интерфейс ключей и данных.
- [ ] Подготовить список методических контрольных примеров для ПЗ-07.

## 8. Добавление новых шифров и алгоритмов

- [ ] Сначала закрыть новые шифры из ПЗ-06.
- [ ] Затем закрыть новые шифры из ПЗ-07.
- [ ] Для каждого нового шифра добавлять реализацию, `data/`, UI-локализацию и тесты как единый комплект.
- [ ] Для каждого нового шифра сразу обновлять документацию проекта.
- [ ] После каждого добавленного шифра прогонять автотесты и ручную проверку UI.

## 9. Финальная сверка перед следующей итерацией

- [ ] Обновить общую матрицу покрытия шифров после внесённых изменений.
- [ ] Проверить, что документация снова согласована с `src/`, `data/`, `tests/` и UI.
- [ ] Проверить, что список реализованных тем совпадает с охватом методичек.
- [ ] Зафиксировать, какие темы остаются вне текущего объёма проекта.
